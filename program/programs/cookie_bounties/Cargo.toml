[package]
name = "cookie_bounties"
version = "0.1.0"
description = "Cookie Bounties — on-chain bounty escrow program for Cookie Chain"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "cookie_bounties"

[features]
default = []
cpi = ["no-entrypoint"]
no-entrypoint = []
no-idl = []
no-log-ix-name = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.30.1"
